# PyPath-Back

Python backend для Swagger API находится в `backend.py`.

## Запуск

```bash
cd <project-root>
python backend.py
```

Сервер поднимается на `http://localhost:3000` и реализует эндпоинты из `swagger.json`.
